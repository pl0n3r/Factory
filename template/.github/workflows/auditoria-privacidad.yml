name: Auditoría de privacidad

on:
  workflow_dispatch:
  schedule:
    - cron: '41 7 * * 1'

permissions:
  contents: read
  issues: write

jobs:
  auditoria:
    uses: pl0n3r/factory/.github/workflows/auditoria-privacidad.yml@v1
    with:
      kit_ref: v1
      label_language: es
